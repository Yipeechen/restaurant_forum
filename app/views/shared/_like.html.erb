 <% if restaurant.is_liked?(current_user) %>
  <%= link_to '<i class="glyphicon glyphicon-thumbs-up" aria-hidden="true"></i>'.html_safe, unlike_restaurant_path(restaurant), method: :post, class: "btn btn-link btn-lg", style:'color: gray ' %>
<% else %>
  <%= link_to '<i class="glyphicon glyphicon-thumbs-up" aria-hidden="true"></i>'.html_safe, like_restaurant_path(restaurant), method: :post, class: "btn btn-link btn-lg", style:'color: blue ' %>
<% end %>